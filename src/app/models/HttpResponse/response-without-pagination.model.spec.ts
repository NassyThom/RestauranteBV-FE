import { ResponseWithoutPagination } from './response-without-pagination.model';

describe('ResponseWithoutPagination', () => {
  it('should create an instance', () => {
    expect(new ResponseWithoutPagination()).toBeTruthy();
  });
});
