import { ResponseWithoutData } from './response-without-data.model';

describe('ResponseWithoutData', () => {
  it('should create an instance', () => {
    expect(new ResponseWithoutData()).toBeTruthy();
  });
});
